---
import { getRecentPosts } from "../services/post";

const posts = await getRecentPosts();
---

<Fragment>
  {
    posts.map((post) => (
      <div>
        <h2>{post.title}</h2>
        <p>{post.body}</p>
      </div>
    ))
  }
</Fragment>
